[project]
name = "audio-signal-plot-app"
version = "0.1.0"
description = "Realtime microphone waveform and spectrum plotter for mobile browsers."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
